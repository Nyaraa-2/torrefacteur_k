import 'package:flutter/cupertino.dart';

class CraftAssemblyPage extends StatelessWidget {
  const CraftAssemblyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
